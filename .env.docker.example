# ProofVault Docker Environment Configuration
# Copy this file to .env.docker and fill in your actual values

# =============================================================================
# Database Configuration
# =============================================================================
# POSTGRES_PASSWORD: Sets the password for the PostgreSQL database user
# This is used during initial database creation and for API connections
# IMPORTANT: Use a strong password for production deployments
POSTGRES_PASSWORD=your_secure_password_here

# =============================================================================
# JWT Configuration
# =============================================================================
# JWT_SECRET: Secret key used for signing JSON Web Tokens
# IMPORTANT: Generate a strong random secret for production
# Example: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here

# =============================================================================
# Digital Evidence API Configuration (Constellation Network)
# =============================================================================
# These credentials are OPTIONAL and should be configured AFTER installation
# when you're ready to use blockchain verification features.
#
# The application works perfectly fine WITHOUT these credentials in local mode.
# You only need to fill these in when you want to:
#   - Register evidence on Constellation Network blockchain
#   - Use cryptographic timestamping
#   - Enable public verification of evidence
#
# Get your credentials at: https://digitalevidence.constellationnetwork.io/
#
# Leave these empty (or with placeholder values) for local development/testing
DE_API_KEY=your_digital_evidence_api_key_here
DE_ORGANIZATION_ID=your_organization_id_here
DE_TENANT_ID=your_tenant_id_here
